import Game from "..";

test('Game init should log "Game init"', () => {
  console.log = jest.fn();
  // const game = new Game();
  // game.init();
  // expect(console.log).toHaveBeenCalledWith("Game init");
});
